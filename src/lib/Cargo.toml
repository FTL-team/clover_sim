[package]
name = "cloversim_lib"
version = "0.1.0"
edition = "2021"

[dependencies]
async-broadcast = "0.5.1"
async-trait = "0.1.60"

serde = { version = "1.0", features = ["derive"] }
ts-rs = "6.2.1"
